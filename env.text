# ENV SETUP (Client)
# Copy these into .env.local for local dev, or .env for CI/builds.

# === REQUIRED ===
# Base URL of your backend API. MUST include the /api suffix.
# Examples:
#   Local:      http://localhost:5001/api
#   Staging:    https://your-backend-staging.onrender.com/api
#   Production: https://api.your-domain.com/api
NEXT_PUBLIC_API_URL=https://grindflow-server-1.onrender.com

# === OPTIONAL ===
# If you enable client-side Socket.IO later, you can set a base URL for it.
# Not used by default in this project, but left here for convenience.
# NEXT_PUBLIC_BASE_URL=http://localhost:5001